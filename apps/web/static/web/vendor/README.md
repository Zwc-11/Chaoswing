This directory is reserved for vendored browser assets if ChaosWing moves away from CDN-loaded libraries.

The current interface preview loads Cytoscape.js from a pinned CDN URL to keep the repository lean while the product surface is still changing quickly.
